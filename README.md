# java-calculator-precourse

## 📢 설명

### 🧐 구현 시 고려 사항

- 모든 구현 시 이유 생각하기
- SOLID 원칙을 고려하며 설계한다
    - 각 객체는 단일한 책임을 가진다
    - 인터페이스를 구현해 개방-폐쇄 원칙을 따른다
    - 추상성이 높은 클래스와 의존관계를 맺는다
- 트레이드 오프를 생각한다
- 행동 단위의 테스트 코드를 작성한다
- 기능 요구 사항 외 내용은 최대한 여러 경우를 고려한다
- 이후 확장성을 고려한다

---

## 🧮 문자열 계산기

### 📁 디렉토리 구조

```
├── controller
│   └── AddCalculatorController.java: 문자열 덧셈 계산기 프로그램 제어
├── error
│   └── ErrorMessage.java: 에러 메세지
├── service
│   ├── Calculator.java: 문자열 덧셈 계산 서비스 인터페이스
│   └── AddCalculator.java: 문자열 덧셈 계산 서비스
├── utils
│   ├── parser
│   │   ├── DividerParser.java: 구분자 추출 파서 인터페이스 
│   │   ├── CustomDividerParser.java: 구분자 추출 파서 
│   │   ├── NumberParser.java: 문자 추출 파서 인터페이스
│   │   └── CustomNumberParser.java: 문자 추출 파서
│   ├── splitter
│   │   ├── InputSplitter.java: 사용자 입력 문자열 splitter 인터페이스
│   │   └── CustomInputSplitter.java: 사용자 입력 문자열 splitter
├── view
│   ├── CalculatorView.java: 문자열 덧셈 계산기 뷰 인터페이스
│   └── CustomCalculatorView.java: 문자열 덧셈 계산기 뷰
└── Application.java

```

### 📝 기능 목록

- 입력
    1. 입력값에 커스텀 구분자 유무를 확인한다.
        1. 커스텀 구분자가 있으면 정해진 형식에 맞게 지정되었는지 확인한다.
            1. 정해진 형식이라면 구분자를 추출한다.
            2. 아니라면 에러를 발생시킨다.
        2. 구분자가 없으면 사용자 전체 입력을 숫자 문자열로 취급한다.
    2. 숫자 문자열에 대해서 구분자를 이용해 입력값을 수열로 변환한다.
        1. 커스텀 구분자, 기본 구분자가 아닌 숫자 문자열에 대해 에러를 발생시킨다.
- 덧셈
    1. 수열의 합을 더한다.
- 출력
    1. 정해진 형식에 맞게 값을 출력한다.

### ✅ 추가 요구 사항 관련 체크리스트

1. 입력
    - [x] 수열은 최대 2억으로 제한된 숫자로 구성된다.
    - [x] 결과값은 Integer를 벗어나지 않는다.
    - [x] 커스텀 구분자는 최대 10개로 지정한다.
    - [x] 커스텀 구분자 문자열, 수열 외의 다른 입력은 고려하지 않는다.
    - [x] 입력은 `커스텀 구분자 지정` → `수열`순으로 받는다.
    - [x] 입력 간에는 띄어쓰기가 있으면 안된다.
        ```
        입력: "//;\n1;2;3"
        출력: 6 // 설명: 1 + 2 + 3
        ```
        ```
        입력: "//;\n  1;2;3"
        애플리케이션 종료 // 설명: 띄어쓰기
        ```
        ```
        입력: "1;2;3//;\n"
        애플리케이션 종료 // 설명: 옳지 않은 순서
        ```
2. 커스텀 구분자
    - [x] 커스텀 구분자는 여러 개 가능하다. 단, 최대 길이는 1로 강제한다.
        ```
        입력: "//;*\n1;2*3"
        애플리케이션 종료 // 설명: 커스텀 구분자의 최대 길이 초과
        ```
        ```
        입력: "//;\n//*\n1;2*3"
        결과: 6 // 설명: 1 + 2 + 3
        ```
    - [x] 커스텀 구분자가 입력된 상태에도 기본 구문자를 사용할 수 있다.
         ```
         입력: "//;\n1,2:3"
         결과: 6 // 설명: 1 + 2 + 3
         ```
    - [x] 커스텀 구분자는 숫자가 불가능하다.
         ```
         입력: "//2\n12223"
         애플리케이션 종료 // 설명: 숫자로 커스텀 구분자 지정
         ```
    - [x] 커스텀 구분자는 공백, 띄어쓰기가 불가하다.
         ```
         입력: "// \n1 2 3"
         애플리케이션 종료 // 설명: 띄어쓰기 구분자 입력
         ```
         ```
         입력: "//\n123"
         애플리케이션 종료 // 설명: 공백 구분자 입력
         ```
3. 숫자
    - [x] 숫자로 기대되는 칸에 공백이 들어올 경우 0으로 취급한다.
        ```
        입력: "1, ,2"
        애플리케이션 종료 // 설명: 띄어쓰기 입력
        ```
        ```
        입력: "//;\n1;;2;3"
        결과: 6 // 설명: 1 + 0 + 2 + 3
        ```
    - [x] 음수로 구성된 수열은 기대하지 않는다. 음수 입력 시 -는 구분자로 취급한다.
         ```
         입력: "//-\n1,-2,-3"
         출력: 6 // 설명: 1 + 0 + 2 + 0 + 3
         ```
         ```
         입력: "1,-2,-3"
         애플리케이션 종료 // 설명: 허용되지 않은 구분자 사용
         ```